# 系統表關係與操作整理

本文件依據 [`server/index.js`](./server/index.js) 的實際建表 SQL、初始化流程與 API 行為整理。

## 1. 資料表總覽

| 表名 | 主要欄位 | 用途 |
| --- | --- | --- |
| `clients` | `id`, `name`, `created_at` | 客戶選項表 |
| `vendors` | `id`, `name`, `created_at` | 廠家選項表 |
| `products` | `id`, `name`, `created_at` | 產品選項表 |
| `task_tags` | `id`, `name`, `created_at` | 任務標籤選項表 |
| `users` | `id`, `mail`, `password_hash`, `password_salt`, `icon`, `icon_bg`, `username`, `role`, `created_at` | 使用者主檔 |
| `auth_tokens` | `id`, `mail`, `token_hash`, `expires_at`, `created_at` | 登入 token |
| `tasks` | `id`, `client_name`, `vendor_name`, `product_name`, `tag_name`, `scheduled_at`, `follow_up`, `created_at` | 舊任務表，目前主要流程未使用 |
| `task_submissions` | `id`, `client_name`, `vendor_name`, `product_name`, `tag_name`, `start_at`, `end_at`, `follow_up`, `created_by_email`, `created_at` | 任務主表 |
| `task_submission_users` | `id`, `submission_id`, `user_mail`, `created_at` | 任務與關聯用戶，多對多 |
| `task_submission_tags` | `id`, `submission_id`, `tag_name`, `created_at` | 任務與標籤，多對多 |
| `follow_up_statuses` | `id`, `name`, `bg_color`, `created_at` | 跟進狀態主檔 |
| `task_submission_followups` | `id`, `submission_id`, `content`, `status_id`, `status_updated_by`, `status_updated_at`, `created_at` | 任務跟進項目 |
| `task_submission_followup_assignees` | `id`, `followup_id`, `user_mail`, `created_at` | 跟進項目與跟進人，多對多 |
| `meeting_folders` | `id`, `client_name`, `vendor_name`, `product_name`, `meeting_time`, `created_by_email`, `created_at` | 會議資料夾主表 |
| `meeting_records` | `id`, `folder_id`, `file_name`, `file_path`, `mime_type`, `file_content`, `content_text`, `created_at` | 會議檔案記錄 |
| `meeting_reports` | `id`, `folder_id`, `content_text`, `created_by_email`, `created_at`, `updated_at` | AI 會議整合報告 |
| `client_vendor_links` | `id`, `client_name`, `vendor_name`, `created_at` | 客戶與廠家階層索引 |
| `vendor_product_links` | `id`, `vendor_name`, `product_name`, `created_at` | 廠家與產品階層索引 |
| `product_meeting_links` | `id`, `product_name`, `meeting_folder_id`, `created_at` | 產品與會議資料夾階層索引 |

---

## 2. 初始化與預設資料

後端啟動時會：

1. 建立 `innerai` 資料庫
2. 建立上述資料表
3. 執行相容性 `ALTER TABLE`
4. 寫入預設資料

預設資料包含：

- 客戶
- 廠家
- 產品
- 任務標籤
- 跟進狀態

預設跟進狀態：

| 名稱 | 預設底色 |
| --- | --- |
| `未完成` | `#fca5a5` |
| `待處理` | `#fde68a` |
| `已完成` | `#86efac` |

---

## 3. 核心關係圖

### 3.1 任務與跟進

```text
users
  └─ auth_tokens

task_submissions
  ├─ task_submission_users
  ├─ task_submission_tags
  └─ task_submission_followups
       ├─ follow_up_statuses
       └─ task_submission_followup_assignees
```

關係說明：

- `task_submissions` 是任務主表。
- `task_submission_users` 保存任務的關聯用戶。
- `task_submission_tags` 保存任務的多標籤。
- `task_submission_followups` 保存任務底下的多筆跟進內容。
- `task_submission_followup_assignees` 保存每筆跟進內容對應的執行人。
- `follow_up_statuses` 提供跟進狀態名稱與顏色。

程式層規則：

- 跟進人只能從該任務的關聯用戶中選。
- 建立任務時，如果有跟進內容，預設狀態會嘗試設定成 `待處理`。
- 更新跟進狀態時，會寫入 `status_updated_by` 與 `status_updated_at`。

### 3.2 會議資料

```text
meeting_folders
  ├─ meeting_records
  └─ meeting_reports

client_vendor_links
  └─ vendor_product_links
       └─ product_meeting_links
            └─ meeting_folders
```

關係說明：

- `meeting_folders` 代表一次會議的主資料。
- `meeting_records` 代表該會議底下的一到多個檔案。
- `meeting_reports` 代表該會議資料夾的 AI 整合報告。
- 三張 `*_links` 表用來組出：
  - 客戶
  - 廠家
  - 產品
  - 會議

這套索引結構主要服務 `GET /api/meeting-records` 的階層式查詢輸出。

---

## 4. 使用者與驗證資料流

### 4.1 `users`

保存：

- 郵件帳號
- 密碼雜湊與 salt
- 頭像與背景色
- 顯示名稱
- 角色

### 4.2 `auth_tokens`

保存：

- token hash
- 到期時間
- 所屬使用者 mail

登入時：

1. 驗證密碼
2. 刪除既有 token 或過期 token
3. 建立新 token
4. 將 token hash 存入 `auth_tokens`

---

## 5. 會議檔案與 AI 報告資料流

### 5.1 上傳會議檔案

前端送出檔案內容後：

1. 新增 `meeting_folders`
2. 寫入階層索引表：
   - `client_vendor_links`
   - `vendor_product_links`
   - `product_meeting_links`
3. 新增 `meeting_records`

### 5.2 文字抽取

`meeting_records.content_text` 的來源：

- `.txt` 直接以 UTF-8 取出文字
- `.docx` 透過 `mammoth.extractRawText`

### 5.3 AI 整合報告

`POST /api/meeting-reports/:id` 會：

1. 讀取該會議資料夾所有 `content_text`
2. 拼接後送往 Dify
3. 將回傳內容寫入 `meeting_reports`
4. 同一個 `folder_id` 只保留一份報告，透過唯一鍵做 upsert

---

## 6. API 與資料表影響

### 6.1 健康檢查

| API | 影響表 | 說明 |
| --- | --- | --- |
| `GET /api/health` | 無 | 回傳 backend / MySQL / Dify 健康狀態 |

### 6.2 基礎選項表

| API | 影響表 | 操作 |
| --- | --- | --- |
| `GET /api/options/:type` | `clients`, `vendors`, `products`, `task_tags` | 讀取選項 |
| `POST /api/options/:type` | 同上 | 新增選項 |
| `DELETE /api/options/:type?name=...` | 同上 | 刪除選項 |

### 6.3 任務主流程

| API | 影響表 | 操作 |
| --- | --- | --- |
| `POST /api/task-submissions` | `task_submissions`, `task_submission_users`, `task_submission_tags`, `task_submission_followups`, `task_submission_followup_assignees` | 建立任務與所有關聯 |
| `GET /api/task-submissions` | 上述任務表 + `users`, `follow_up_statuses` | 回傳任務、用戶、標籤、跟進與狀態 |
| `PUT /api/task-submissions/:id` | 任務系列表 | 更新任務，先清掉舊關聯再重建 |
| `DELETE /api/task-submissions/:id` | 任務系列表 | 依依賴順序刪除任務資料 |

### 6.4 跟進狀態與指派

| API | 影響表 | 操作 |
| --- | --- | --- |
| `GET /api/follow-up-statuses` | `follow_up_statuses` | 讀取狀態清單 |
| `POST /api/follow-up-statuses` | `follow_up_statuses` | 新增狀態 |
| `PUT /api/follow-up-statuses/:id` | `follow_up_statuses` | 更新名稱或顏色 |
| `DELETE /api/follow-up-statuses/:id` | `follow_up_statuses` | 刪除狀態 |
| `PUT /api/task-submission-followups/:id` | `task_submission_followups`, `task_submission_followup_assignees` | 更新狀態、修改者與跟進人 |

### 6.5 會議記錄

| API | 影響表 | 操作 |
| --- | --- | --- |
| `POST /api/meeting-records` | `meeting_folders`, `meeting_records`, `client_vendor_links`, `vendor_product_links`, `product_meeting_links` | 建立新會議資料夾與檔案 |
| `GET /api/meeting-records` | 會議系列表 | 回傳階層式會議結構 |
| `POST /api/meeting-records/:id` | `meeting_records` | 向既有資料夾追加檔案 |
| `DELETE /api/meeting-records/:id` | `meeting_records`, 視情況清理 `meeting_reports`, `product_meeting_links`, `meeting_folders` | 刪除單筆檔案 |
| `DELETE /api/meeting-folders/:id` | `meeting_records`, `meeting_reports`, `product_meeting_links`, `meeting_folders`, 視情況清理 link 表 | 刪除整個會議資料夾 |
| `POST /api/meeting-reports/:id` | `meeting_reports` | 生成或更新 AI 會議報告 |

### 6.6 AI 提取

| API | 影響表 | 操作 |
| --- | --- | --- |
| `POST /api/dify/auto-fill` | 無直接寫表 | 將會議文字送往 Dify，回傳可供任務表單填寫的內容 |

### 6.7 驗證與使用者維護

| API | 影響表 | 操作 |
| --- | --- | --- |
| `POST /api/auth/request-code` | 記憶體暫存 | 產生註冊驗證碼 |
| `POST /api/auth/register` | `users` | 建立帳號 |
| `POST /api/auth/login` | `auth_tokens` | 建立登入 token |
| `POST /api/auth/logout` | `auth_tokens` | 刪除指定 token |
| `POST /api/auth/verify` | `auth_tokens`, `users` | 驗證 token 並回傳使用者資訊 |
| `GET /api/users` | `users` | 讀取使用者清單 |
| `POST /api/users/update` | `users` | 更新使用者資料或密碼 |

### 6.8 遙測紀錄

| API | 影響表 | 操作 |
| --- | --- | --- |
| `POST /api/telemetry/navigation` | 無 | 記錄頁面進入與 route change 至 log |

---

## 7. 索引與唯一鍵

較重要的索引/唯一鍵：

- `clients.name`, `vendors.name`, `products.name`, `task_tags.name` 唯一
- `users.mail` 唯一
- `auth_tokens.token_hash` 唯一
- `meeting_reports.folder_id` 唯一
- `client_vendor_links(client_name, vendor_name)` 唯一
- `vendor_product_links(vendor_name, product_name)` 唯一
- `product_meeting_links(product_name, meeting_folder_id)` 唯一

查詢索引主要分布在：

- 任務建立時間
- 任務與用戶關聯
- 任務與標籤關聯
- 跟進與指派關聯
- 會議資料夾與記錄關聯

---

## 8. 刪除與資料一致性

目前資料表沒有宣告正式外鍵，關聯一致性主要由程式控制。

### 8.1 刪除任務

刪除順序：

1. `task_submission_followup_assignees`
2. `task_submission_followups`
3. `task_submission_tags`
4. `task_submission_users`
5. `task_submissions`

### 8.2 刪除會議資料夾

刪除順序：

1. `meeting_records`
2. `meeting_reports`
3. `product_meeting_links`
4. `meeting_folders`
5. 若已無任何相同組合資料，再清理：
   - `vendor_product_links`
   - `client_vendor_links`

---

## 9. 備註

- `tasks` 表目前保留，但主流程已轉由 `task_submissions` 系列表處理。
- `task_submissions.tag_name` 與 `follow_up` 欄位目前仍存在，但實際主流程已以關聯表和 `task_submission_followups` 為主。
- 註冊驗證碼存在記憶體中，服務重啟後會清空。
- 寫入型 SQL 會透過審計包裝記錄至日誌，便於追查資料異動。
