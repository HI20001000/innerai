{
  "name": "innerai",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "npm-run-all -p dev:server dev:web",
    "dev:server": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev",
    "server": "npm run dev:server",
    "web": "npm run dev:web",
    "build": "npm-run-all build:server build:web",
    "build:server": "npm --workspace server run build",
    "build:web": "npm --workspace web run build",
    "lint": "npm-run-all lint:server lint:web",
    "lint:server": "npm --workspace server run lint",
    "lint:web": "npm --workspace web run lint",
    "format": "npm-run-all format:server format:web"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
